import { useEffect } from 'react';

export function useScenesGameObjects(gameId: string, sceneId: string) {
    useEffect(() => {
        if (Boolean(gameId) && Boolean(sceneId)) {
            
        }
    }, [gameId, sceneId]);
}